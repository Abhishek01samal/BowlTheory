
import { HeroSection } from "@/components/ui/hero-section";

export const StorySection = () => {
    return (
        <section id="story">
            {/* Full Screen Hero reused as a section break or story intro */}
            <HeroSection />
        </section>
    );
};
